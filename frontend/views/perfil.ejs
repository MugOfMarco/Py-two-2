<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil - π two 2</title>
    <link rel="stylesheet" href="/css/login.css"> <style>
        /* Estilos extra para el perfil */
        .profile-header { text-align: center; margin-bottom: 20px; }
        .readonly-input { background-color: #f0f0f0; color: #666; cursor: not-allowed; }
        .btn-save { background-color: #28a745; color: white; width: 100%; padding: 12px; border: none; border-radius: 8px; font-weight: bold; cursor: pointer; margin-top: 10px;}
        .btn-save:hover { background-color: #218838; }
        .btn-back { display: block; text-align: center; margin-top: 15px; color: #666; text-decoration: none; }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="brand-side">
            <a href="/main"><div class="logo">π two 2</div></a>
            <p class="brand-tagline">Gestiona tu información personal</p>
        </div>

        <div class="login-side">
            <div class="profile-header">
                <h2>Mi Perfil</h2>
                <p class="subtitle">Actualiza tus datos</p>
            </div>

            <form id="profile-form">
                <div class="form-row" style="display: flex; gap: 10px;">
                    <div class="form-group" style="flex: 1;">
                        <label>Nombre</label>
                        <input type="text" id="nombre" name="nombre" class="input-letras" required>
                    </div>
                    <div class="form-group" style="flex: 1;">
                        <label>Apellido</label>
                        <input type="text" id="apellido" name="apellido" class="input-letras" required>
                    </div>
                </div>

                <div class="form-group">
                    <label>Correo electrónico (No editable)</label>
                    <input type="email" id="email" class="readonly-input" readonly>
                </div>

                <div class="form-group">
                    <label>Teléfono</label>
                    <input type="tel" id="telefono" name="telefono" class="input-numeros">
                </div>

                <div class="form-group">
                    <label>Código Postal</label>
                    <input type="text" id="codigo_postal" name="codigo_postal">
                </div>

                <button type="submit" class="btn-save">Guardar Cambios</button>
                
                <a href="/main" class="btn-back">← Volver a la tienda</a>
                <button type="button" id="btn-logout" style="width:100%; margin-top:10px; background:#dc3545; color:white; border:none; padding:10px; border-radius:8px; cursor:pointer;">Cerrar Sesión</button>
            </form>
        </div>
    </div>

    <script src="/js/perfil.js"></script>
</body>
</html>